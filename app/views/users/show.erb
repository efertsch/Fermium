<h1>Welcome to your profile <%=@user.name.capitalize%>! </h1>

<div>
  <p id=profile-questions>
    <%= "You've asked #{@user.questions.count} questions, answered #{@user.answers.count} and made THIS_MANY comments.  Here are some questions you've asked:" %>
    <ul>
      <% limited_questions = @user.questions.limit(5) %>
      <% limited_questions.each do |question| %>
      <li> <a href=<%="/questions/#{question.id}"%>> <%=  question.question_text %></a>
        <% end %>
      </p>
     <%= "Here are some of your answers:" %>
      <% limited_answers = @user.answers.limit(5) %>
      <% limited_answers.each do |answer| %>
      <li><%= answer.answer_text %>
        <% end %>
      <ul>



</div>
